<%= form_with(model: feed, local: true) do |form| %>
  <div class="field">
    <%= form.label :url %>
    <%= form.text_area :url %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
