<div class="container">
	<h1><%= @feed.title %></h1>
	<%= image_tag @feed.image unless @feed.image.blank? %>
	<%= link_to 'Edit', edit_feed_path(@feed) %>
	<%= link_to 'Destroy', feed_path(@feed), method: :delete %>
</div>
<% @feed.entries.each do |entry| %>
  <%= render entry %>
<% end %>
